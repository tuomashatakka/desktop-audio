# Feature: Update Hero Section

## Overview
Update the public website's hero section with:
1. More prominent sine waves in the hero canvas
2. Typewriter effect for all hero text content (including title)
3. Hide caret until text is being appended
4. Start icon line transitions only after section transitions in

## Tasks

### Task 1: Make sine waves more prominent
**File**: `public/index.html` (drawEffects function, lines ~944-957)
- Increase alpha values for both sine waves (currently 0.3 and 0.2)
- Increase amplitude (currently 25 and 18)
- Possibly brighten colors for better visibility
- Target: sine waves should be clearly visible against background

### Task 2: Add typewriter effect to hero title
**File**: `public/index.html` (hero section HTML + typeText function)
- Add `data-html` attribute to `<h1 class="hero-title">Desktop Audio</h1>`
- Add `.typed-text` and `.typing-cursor` spans inside the h1
- Update typeText() to also process the hero title element
- Ensure title types in before paragraphs (adjust timing)

### Task 3: Hide caret until text is being appended
**File**: `public/main.css` + `public/index.html`
- CSS: Add `.typing-cursor { display: none; }` by default
- JS: Show cursor via `display: inline-block` when typing starts for each line
- JS: Hide cursor again after typing completes (existing behavior)

### Task 4: Delay icon line transitions
**File**: `public/main.css` (lines ~353-364)
- Verify icon transitions only start when `.feature-card.visible` is added
- Ensure the transition doesn't start before the section is visible
- The `stroke-dashoffset` transition should only trigger on `visible` class

## Implementation Order
1. Task 1: Sine waves (independent)
2. Task 2: Typewriter title (depends on understanding typeText function)
3. Task 3: Caret hiding (depends on Task 2)
4. Task 4: Icon transitions (independent CSS change)

## Files to Modify
- `public/index.html` - sine waves, typewriter, caret
- `public/main.css` - caret styling, icon transitions
